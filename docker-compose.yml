services:
  bot:
    build: .
    container_name: larkbot
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./logs:/app/logs
      - ./hooks_custom:/app/hooks_custom
      - ${HOME}/.lark-cli:/root/.lark-cli:ro
    stop_grace_period: 15s
